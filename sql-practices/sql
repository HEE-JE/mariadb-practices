-- Basic Query

select version(), current_date;

-- 대소문 구분이 없다.
SELECT VERSION(), CURRENT_DATE;
select VeRsIoN(), current_DATE;

-- 수학함수
select sin(pi()/4), (4+1)*5;

-- 테이블 생성
create table pet(
	name varchar(
)